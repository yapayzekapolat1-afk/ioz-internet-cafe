{
  "name": "ioz-internet-cafe",
  "version": "1.0.0",
  "private": true,
  "description": "iOZ Internet Cafe - internet cafe isletmeciligi simulasyonu",
  "scripts": {
    "sync": "cap sync android",
    "build:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
